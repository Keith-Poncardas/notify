<nav class="navbar bg-body-tertiary sticky-top shadow-sm">
  <div class="container">
    <!-- Brand -->
    <div class="navbar-brand">
      <%- include('./brand', { src: '/img/brand.png', location: '/' }); %>
    </div>

    <% if (user) { %>
    <!-- Search Form - Always visible, centered -->
    <div class="flex-fill mx-3 mx-md-4" style="max-width: 400px;">
      <%- include('./searchForm', { placeholder: 'Search' }); %>
    </div>

    <!-- Right side actions -->
    <div class="d-flex align-items-center gap-3">
      <a class="text-muted" href="/users" title="Browse Users">
        <i class="fas fa-users fs-5"></i>
      </a>
      <%- include('./avatarDropdown') %>
    </div>
    <% } else { %>
    <!-- Not Logged In -->
    <div class="d-flex align-items-center gap-2">
      <button type="button" class="btn btn-sm btn-link text-muted" data-mdb-toggle="modal" data-mdb-target="#loginModal">
        Login
      </button>
      <button type="button" class="btn btn-sm btn-primary" data-mdb-toggle="modal" data-mdb-target="#signupModal">
        Sign up
      </button>
    </div>
    <% } %>
  </div>
</nav>